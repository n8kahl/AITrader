{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cd apps/gateway && npm ci && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd apps/gateway && npm start",
    "healthcheckPath": "/api/health",
    "port": 8080
  }
}
